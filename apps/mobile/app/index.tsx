/**
 * Index Screen
 *
 * หน้านี้เป็น route เริ่มต้นของแอป
 *
 * สิ่งที่เกิดขึ้นในหน้านี้:
 * - ไม่แสดง UI ใด ๆ
 * - ปล่อยให้ route guard ตัดสินใจว่าจะพาผู้ใช้ไปหน้าไหน
 * - เช่น Auth, Setup หรือ Dashboard
 */

export default function Index() {
  return null;
}
